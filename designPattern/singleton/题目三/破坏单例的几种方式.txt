
破坏单例模式的方法目前我知道的有两种。

1、反射
2、序列化和反序列化

	针对反射：虽然单例模式将构造方法私有化，但是对于反射来说仍是透明的。我们可以在构造方法中加入对实例的判断，若实例已经存在了就不允许在创建对象，并抛出异常。
这样就可以阻止被人恶意破坏。为了防止反射破坏，我们也可以采用枚举式单例来屏蔽反射。

	针对序列化和反序列化：序列化和反序列化会再次创建该类的实例，所以针对这种情况，我们可以选在重写readRsolve方法来解决，也可以使用枚举式单例。

